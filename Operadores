fun main() {
    // Cambiamos las variables a flotantes por que nuestro resultado es en decimal
    val x1 : Float=4f
    val x2 : Float=-3f
    val y1 : Float=3f
    val y2: Float=-2f
  
    val resultado = (y2-y1)/(x2-x1)
    println("El resultado es: $resultado")
}
